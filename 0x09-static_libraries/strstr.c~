#include "holberton.h"

/**
 * _strncat - check the code for Holberton School students.
 * @haystack: Pointer
 * @needle: Pointer
 * Return: Always 0.
 */
char *_strstr(char *haystack, char *needle)
{
	char i = *haystack;
	char j = *needle;

	return (i);
	return (j);
}
