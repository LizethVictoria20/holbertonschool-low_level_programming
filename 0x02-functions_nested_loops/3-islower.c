#include <unistd.h>
#include "holberton.h"
/**
 * _islower - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int _islower(int c)
{
	if (_islower(c))
		return (c + '1');
	else
		return (c + '0');
}
