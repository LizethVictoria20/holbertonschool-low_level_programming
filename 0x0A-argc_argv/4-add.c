#include <stdio.h>
#include <stdlib.h>

int only_number(*s)
{
	while (*s[i] != 0)
	{
		if (*s[i] >= 0 && *s[i] >= 9)
		{
			i++;
		}
		else
		{
			return (0);
		}
	}
	return (1);
}

int main(int argc, char **argv)
{
	int i, suma;

	if (argv[i] == 1)
	{
		printf("0\n");
	}
	
}
